当任务在试图预测数值时，最常见的损失函数是*平方误差*（squared error），即预测值与实际值之差的平方。

线性转换+激活函数的堆叠

逻辑回归

二分类

多分类

不管是多酚类还是二分类，首先要做的第一步都是线性转换

wx+b   softmax

四个特征 三个类别的概率值 当作样本新的特征

神经元数量与模型强弱有相关性

用组合线来分割数据

映射到新的空间上，又是线性可分的

激活函数一般都是单调的输出范围可控

一般情况下，神经网络最后一层不做概率转换，只做决策

中间层一般都做激活函数

层数较多深层，层数较少浅层

softmax

正向 反向

FP BP

线性转换 + 激活函数

线性转换

思考网络结构：正向过程（线性转换 + 激活函数）

基于损失算梯度：反向过程

预测值与正式值求损失函数，求导，然后反过来更新参数

梯度下降（复习）

雅可比矩阵

黎曼和

[https://www.bilibili.com/video/BV1EDDuYtEZf/?spm_id_from=333.337.search-card.all.click&vd_source=ff56ee0944e3a219cfe0ba0491a956f5](https://www.bilibili.com/video/BV1EDDuYtEZf/?spm_id_from=333.337.search-card.all.click&vd_source=ff56ee0944e3a219cfe0ba0491a956f5)

pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 -i [https://mirrors.aliyun.com/pypi/simple/](https://mirrors.aliyun.com/pypi/simple/)

pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 -i [https://mirrors.aliyun.com/pypi/simple/](https://mirrors.aliyun.com/pypi/simple/)

：PyTorch框架熟悉、参考机器学习的模型训练代码-->说一下深度学习的模型训练

全联接神经网络

深度学习模型的基础代码

BP神经网络

学习率就是步长，每次下降多少

学习率可能会导致数据出现抖动的情况

Adam 最优步长

在神经网络中，梯度下降有一个专门的名称叫做BP也就是指的是优化方式；

神经网络的内部只有两个结构：线性转换+激活函数；

预测值与实际值算损失。

x如果是一行一个样本，xw+b，如果是一列一个样本，wx+b；

为什么要转置，跟框架定义有关，算子区分，第一维永远表示输出；

[https://yuanbao.tencent.com/chat/naQivTmsDa/20811cec-ae84-41bf-ad8a-a1da701ddc4e](https://yuanbao.tencent.com/chat/naQivTmsDa/20811cec-ae84-41bf-ad8a-a1da701ddc4e)

**PyTorch如何实现自动微分**

transformer手写推理过程

图像 语义分割 实例分割

实例是基于个体，语义是基于类别

Semantic Segmentation

Instance Segmentation

深度学习：通过数据训练得出模型

泛化能力（举一反三）

卷积层

池化层